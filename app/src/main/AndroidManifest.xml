<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.upm.dit.adsw">

    <!-- Cambiar intent-filter para lanzar una aplicación u otra -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivityListArrayAdapter">

        </activity>
        <activity android:name=".ActivityRecyclerView">

        </activity>
        <activity android:name=".ActivityListSimpleAdapter">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityListCustomAdapter">

        </activity>
    </application>

</manifest>